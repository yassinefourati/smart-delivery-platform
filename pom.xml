<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.smartdelivery</groupId>
    <artifactId>smart-delivery-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Smart Delivery Platform</name>
    <description>Microservices-based smart logistics and delivery management platform</description>

    <modules>
        <!--
          First, because every service module depends on it (Phase 19, ADR 009). It holds
          the infrastructure each service used to carry its own copy of; it holds nothing
          domain-shaped, and the ADR says why that boundary is where it is.
        -->
        <module>platform-starter</module>
        <module>api-gateway</module>
        <module>user-service</module>
        <module>product-service</module>
        <module>inventory-service</module>
        <module>order-service</module>
        <module>payment-service</module>
        <module>delivery-service</module>
        <module>notification-service</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>

        <spring-boot.version>3.5.13</spring-boot.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>

        <!-- Libraries added incrementally as each service's phase is implemented -->
        <springdoc-openapi.version>2.8.5</springdoc-openapi.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <testcontainers.version>1.20.4</testcontainers.version>

        <!-- Build-quality tooling (Phase 18) -->
        <jacoco.version>0.8.12</jacoco.version>
        <archunit.version>1.3.0</archunit.version>
        <spotbugs-maven-plugin.version>4.8.6.6</spotbugs-maven-plugin.version>
        <!--
          Per-module line-coverage floor, enforced by jacoco:check. Each module overrides
          this with its own measured value rounded down; see docs/ci-cd.md. The default
          here is 0 so a module that has not been measured yet (or has no code, like the
          aggregator) cannot fail the build on a number nobody chose.
        -->
        <jacoco.line.coverage.minimum>0.00</jacoco.line.coverage.minimum>

        <!--
          Security overrides of versions the Boot and Cloud BOMs would otherwise pick. The
          image scan (Trivy, CRITICAL, in CI) fails on the versions those BOMs ship with:
          Tomcat 10.1.53 (six CVEs fixed by 10.1.58; 10.1.58 itself was never published),
          Netty 4.1.132 (CVE-2026-75595, fixed in 4.1.137) and BouncyCastle 1.80, which
          spring-cloud-starter pulls in (CVE-2025-14813, CVE-2026-8763, fixed in 1.85).
          Each one is the same minor line the BOM chose, so these are patch bumps, not
          upgrades. Drop an entry once spring-boot.version / spring-cloud.version ships an
          equal or newer version; a stale override would otherwise pin us backwards.
        -->
        <tomcat.version>10.1.60</tomcat.version>
        <netty.version>4.1.138.Final</netty.version>
        <bouncycastle.version>1.86</bouncycastle.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
              The overrides above. Boot's versions arrive through an imported BOM, and an
              imported BOM's own properties cannot be overridden from here (that only works
              with spring-boot-starter-parent), so the versions are pinned directly: explicit
              entries always beat imported ones, and among imports the first one declared
              wins, which is why netty-bom sits before spring-boot-dependencies.
            -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <!--
          Inherited by every module (Phase 18). JaCoCo measures what the tests actually
          reach and fails the build if a module drops below the floor it was measured at.
          The point is to stop coverage sliding, not to chase a number.
        -->
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>check-line-coverage</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <rule>
                                        <element>BUNDLE</element>
                                        <limits>
                                            <limit>
                                                <counter>LINE</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>${jacoco.line.coverage.minimum}</minimum>
                                            </limit>
                                        </limits>
                                    </rule>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs-maven-plugin.version}</version>
                    <configuration>
                        <effort>Max</effort>
                        <threshold>Medium</threshold>
                        <includeTests>false</includeTests>
                        <excludeFilterFile>${maven.multiModuleProjectDirectory}/spotbugs-exclude.xml</excludeFilterFile>
                    </configuration>
                    <executions>
                        <execution>
                            <id>analyse</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!--
                  The repackage execution is declared explicitly because this build does
                  NOT inherit spring-boot-starter-parent (it imports the BOM instead, see
                  dependencyManagement above), and nothing else binds the goal. Without
                  it `mvn package` produces a plain library jar with no Main-Class, so
                  every Dockerfile's `java -jar app.jar` fails with "no main manifest
                  attribute" the moment the container starts. Found in Phase 18, when the
                  end-to-end smoke test needed the stack to actually run for the first
                  time; CI had only ever built the images, never started one.
                -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <executions>
                        <execution>
                            <id>repackage</id>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
